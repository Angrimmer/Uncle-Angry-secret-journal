<div class="uasj-magasin-view">

    <div class="uasj-magasin-header">
        <div class="uasj-magasin-icone-wrap">
            {{#if src}}
            <img class="uasj-magasin-icone" src="{{src}}" alt="{{name}}"
                 data-action="uasjAgrandirIcone" data-tooltip="{{localize 'UASJ.Image.Agrandir'}}">
            {{/if}}
        </div>
        <div class="uasj-magasin-header-right">
            <h1 class="uasj-magasin-nom">{{name}}</h1>
            {{#if infos.length}}
            <div class="uasj-infos-canvas{{#if isOwner}}{{#unless infosVerrouillees}} uasj-draggable{{/unless}}{{/if}}">
                {{#each infos}}
                {{#if this.valeur}}
                <span class="uasj-info-chip" data-info-id="{{this.id}}" style="left: {{this.x}}%; top: {{this.y}}%;">
                    <strong>{{localize this.label}}</strong> {{this.valeur}}
                </span>
                {{/if}}
                {{/each}}
            </div>
            {{/if}}
        </div>
    </div>

    <nav class="uasj-tabs tabs" data-group="primary">
        {{#each tabs}}
        <a class="uasj-tab-link {{this.cssClass}}" data-action="tab" data-tab="{{this.id}}" data-group="{{this.group}}">
            <i class="{{this.icon}}"></i> {{localize this.label}}
        </a>
        {{/each}}
    </nav>

    <div class="tab uasj-magasin-tab-description{{#if tabs.description.active}} active{{/if}}"
         data-group="primary" data-tab="description">
        {{{description}}}
    </div>

    <div class="tab uasj-magasin-tab-offerings{{#if tabs.offerings.active}} active{{/if}}"
         data-group="primary" data-tab="offerings">
        {{> "modules/uncle-angry-secret-journal/templates/partials/offerings-panel-view.hbs"}}
    </div>

    <div class="tab uasj-magasin-tab-relations{{#if tabs.relations.active}} active{{/if}}"
         data-group="primary" data-tab="relations">
        {{> "modules/uncle-angry-secret-journal/templates/partials/relations-panel-view.hbs"}}
    </div>

</div>
